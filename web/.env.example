# 後端位址。開發時後端跑在 :3000，本站跑在 :3001。
NEXT_PUBLIC_API_BASE=http://localhost:3000

# 開發用身分（FakeLineAdapter 格式）。
# 正式環境改由 LIFF 取得 id_token，見 lib/api.ts 的 TODO(liff)。
NEXT_PUBLIC_DEV_TOKEN=id-token:U-dev-guardian-yijun
